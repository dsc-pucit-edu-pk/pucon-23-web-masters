interface ButtonProps {
   color:
      | "black"
      | "white"
      | "primary"
      | "secondary"
      | "success"
      | "warning"
      | "gray";
}

const Button: React.FC<ButtonProps> = () => {
   return <></>;
};

export default Button;
