/** @type {import('next').NextConfig} */
const nextConfig = {
   // output: 'export',
   // distDir: 'dist',
   // reactStrictMode: true,
   // // basePath: '/',
   // assetPrefix: '../',
   // images: {
   //   unoptimized: true
   // }
};

module.exports = nextConfig;
